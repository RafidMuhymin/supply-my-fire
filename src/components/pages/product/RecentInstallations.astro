---
import Installations from "@/components/Installations.astro";

const { title, installations, button } = (
  (await import(
    `@/data/products/${Astro.params.product}/recent-installations.ts`
  )) as typeof import("@/data/products/media-wall/recent-installations.ts")
).default;
---

<Installations {title} {installations} {button} />
