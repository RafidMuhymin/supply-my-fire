---
import Installation from "@/components/Installation.astro";

const { title, installations, button } = (
  (await import(
    `@/data/locations/${Astro.params.location}/recent-installations.ts`
  )) as typeof import("@/data/locations/cardiff/recent-installations.ts")
).default;
---

<Installation {title} {installations} {button} />
